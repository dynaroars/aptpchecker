general:
  enable_incomplete_verification: False
model:
  onnx_quirks: "{'Reshape': {'fix_batch_size': True}}"
solver:
  crown:
    batch_size: 256
bab:
  initial_max_domains: 1
  branching:
    input_split:
      enable: False
attack:
  pgd_order: before
