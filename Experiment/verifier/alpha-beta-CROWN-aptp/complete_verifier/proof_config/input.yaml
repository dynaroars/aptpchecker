general:
    device: cuda
model:
  onnx_quirks: "{'Reshape': {'fix_batch_size': True}}"
solver:
  bound_prop_method: crown
  crown:
    batch_size: 512
bab:
  # initial_max_domains: 1
  branching:
    method: naive 
    input_split:
      enable: True
attack:
  pgd_order: before
